# Techma Counting — Gateway releases

Public release channel for the Techma counting gateway (self-contained binaries + auto-update manifest). Install or upgrade a device:

```
curl -fsSL https://raw.githubusercontent.com/techma-technology/counting-gateway/main/install.sh | sudo bash
```
